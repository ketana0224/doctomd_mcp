# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: mcp-pdf-pipeline
metadata:
  template: mcp-pdf-pipeline@0.1.0

services:
  api:
    project: ./src
    language: python
    host: function
